import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sriramcharankolla.devops',
  appName: 'DevOps Mobile',
  webDir: 'www'
};

export default config;
